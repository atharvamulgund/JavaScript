var array =[56,"Whatsapp",true,null,"xyz"]

array.pop()//Only removes the last elements 

array.push("Senapi")//adds the element to the last

array.shift()//start's the array from 1 by removing 0

array.unshift("Bot")//it adds the element to the start even if the above methods are not used

array.toString()//it converts the array in string

array.sort();//It sorts the array

console.log(array)